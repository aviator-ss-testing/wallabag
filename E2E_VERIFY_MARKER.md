# E2E Verify Marker

This file is an Aviator verify end-to-end test marker.

It was added by the `verify-shaped-mcp` e2e run to exercise the verify flow
(intent + acceptance criteria) against a real draft-gated pull request.

Marker: verify-shaped-mcp e2e
