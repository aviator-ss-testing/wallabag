# E2B sandbox template config for the wallabag preview image.
#
# This rebuilds the EXISTING template in place (template_id below), so the
# mergeit preview config (image: walladev-preinstall-deps -> account-1-id-6)
# keeps resolving to it with no repoint. team_id is the aviator-prod team
# (matches extras/e2b_templates/e2b.prod.toml in mergeit).
#
# Build:  bash docker/preview/build.sh e2b

team_id = "b7021a8c-0dc0-4514-acb1-377d3dd1d8e3"
memory_mb = 4_096
cpu_count = 4
dockerfile = "docker/preview/Dockerfile"
template_name = "account-1-id-6"
template_id = "re9xmn9s1vl40oqxl2ds"
